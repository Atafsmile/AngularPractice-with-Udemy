import { BatterHighLightDirective } from './batter-high-light.directive';

describe('BatterHighLightDirective', () => {
  it('should create an instance', () => {
    const directive = new BatterHighLightDirective();
    expect(directive).toBeTruthy();
  });
});
